<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fennec.allojib">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.Order_Plat_Activity"></activity>
        <activity android:name=".controller.Restaurant_Activity" />
        <activity
            android:name=".controller.CategoryPlat_Activity"
            android:label="@string/title_activity_category_plat_"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".controller.Register_form" />
        <activity android:name=".controller.Menu_Activity" />
        <activity android:name=".controller.Wait_Activity" />
        <activity android:name=".controller.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>