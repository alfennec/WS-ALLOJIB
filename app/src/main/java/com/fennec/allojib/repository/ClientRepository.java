package com.fennec.allojib.repository;

import com.fennec.allojib.entity.Client;

import java.util.ArrayList;

public class ClientRepository {

    public static Client main_Client = new Client();

   /* public static void fixture()
    {
        list_Client.add(new Client(1,"bermed@bermed.com", "bermed", "bermed", "bermed", "0611336625", "118 hay adnalous oujda", "Oujda", 1));
    }

    public static Client getById(int id)
    {
        Client current_Client = new Client();

        for (int i=0; i<list_Client.size(); i++)
        {
            if(id == list_Client.get(i).id)
            {
                current_Client=list_Client.get(i);
            }
        }

        return current_Client;
    }*/
}
